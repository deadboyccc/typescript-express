import express from "express"
// import * as views from "../controllers/viewControllers"
// import { protect } from "../controllers/authenticationController"

export const viewRouter = express.Router()

// viewRouter.get("/", protect, views.overviewController)
// viewRouter.get("/tour/:id", protect, views.tourController)
// viewRouter.get("/login", views.login)
// viewRouter.get("/update", protect, views.updateUser)
